# TOTO List

- [x] Log need to be added, use proper logging package, and log to file instead of printing to console, professional log format, with timestamp, log level, and message. Log levels: DEBUG, INFO, WARNING, ERROR, CRITICAL. Both frontend and backend should have logs in separate files, but with consistent format. Online games' hosts will have both, clients will have frontend log. 

- [x] The game lobby should not have dropdown list for agent checkpoints and AI speed if the slot is not assigned to AI agent. 

- [x] Add all 4 script bot variants to the menu for the player to play against.